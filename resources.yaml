---

resources:
  build/native-build-headers:
    phases: native:install
    version: latest
    path: $RO_NATIVE

exports:
  build/cross/basictokenise:
    phases: native:install
    upload: false
    path: $ROTOOL_DIR
    files:
      - riscos-basictokenise
